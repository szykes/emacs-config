# -*- mode: snippet -*-
# name: frame - recommended comment sections for the standard elisp file
# key: frame
# --
;;; `(file-name-nondirectory (buffer-file-name))` --- $1

;;; Commentary:

;;; Code:

$0

(provide '`(file-name-base (buffer-file-name))`)
;;; `(file-name-nondirectory (buffer-file-name))` ends here